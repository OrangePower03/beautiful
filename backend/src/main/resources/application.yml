

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/backend
    hikari:
      username: root
      password: 123123
      driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false

  # 值得注意，执行到这就不会继续执行下面的命令了
  profiles:
    active: dev

