                 // 加油哟，写有一个带有用户头像的，作品排列在下面的或者哪哪都可以
// 然后，你可以照抄tkn写的 SearchArtworkResult 那个界面
// 我传的数据是如下，麻烦你把这些数据都整进那个界面，具体含义你去tkn写的那里看吧，说实话我也稍微看不懂

interface ArtworkDto {
    id: number
    atitle:string
    title: string
    avatar: string
    intro: string
    ip: string
    kind: string
    resourceAddress:string
    userName:string
}
export default function UserArtwork(){
    return <div>
        用户界面
    </div>

}

